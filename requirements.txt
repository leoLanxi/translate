# 日语视频字幕自动生成工具 v3.0 - Python 依赖列表
# =============================================
#
# 安装方式：
#   pip install -r requirements.txt
#
# ========== 跨平台支持 ==========
#
# macOS (Apple Silicon M1/M2/M3):
#   - 自动使用 CPU 模式
#   - 如果遇到安装问题，请确保 Xcode Command Line Tools 已安装：
#     xcode-select --install
#
# Windows (NVIDIA GPU - 推荐 RTX 30/40/50 系列):
#   - 自动检测 CUDA，启用 GPU 加速（速度提升 5-10 倍）
#   - 额外安装 CUDA 支持（可选，通常自动安装）：
#     pip install nvidia-cublas-cu11 nvidia-cudnn-cu11
#
# Linux (NVIDIA GPU):
#   - 同 Windows，自动启用 CUDA 加速

# ========== 核心依赖 ==========

# faster-whisper 语音识别库
# 比 OpenAI 原版 whisper 快 4-5 倍，内存占用更少
faster-whisper>=1.0.0

# 翻译库：用于生成中文字幕（日语 → 中文翻译）
# 使用 Google 翻译免费接口
deep-translator>=1.11.0

# 进度条显示
tqdm>=4.60.0

# ========== 自动安装的依赖 ==========
# faster-whisper 会自动安装以下依赖：
# - ctranslate2: 高性能推理引擎
# - huggingface_hub: 模型下载
# - tokenizers: 分词器
# - av: 音视频处理
# - numpy: 数值计算

